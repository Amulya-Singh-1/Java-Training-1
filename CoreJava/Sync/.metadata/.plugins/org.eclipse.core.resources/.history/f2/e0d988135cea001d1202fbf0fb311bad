package CollectionsPractise;

import java.util.*;

public class EgLinkedHashSet {
	public static void main(String[] args) {
//		HashSet<Integer> myset=new HashSet<>();      // jumbled order in output
//		TreeSet<Integer> myset=new TreeSet<>();		 // maintains a default or custom sorting order 
		LinkedHashSet<Integer> myset=new LinkedHashSet<>();    // maintains the insertion order
		myset.add(20);
		myset.add(10);
		myset.add(30);
		myset.add(50);
		myset.add(40);
		
		System.out.println(myset);
		
	}
}
